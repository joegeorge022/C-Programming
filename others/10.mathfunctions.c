#include <stdio.h>
#include <math.h>

int main(){

    double A = sqrt(9);     // square root 
    double B =pow(2,5);     // power
    int C = round(3.14);    // round off
    int D = ceil(3.14);     // round off to higher value
    int E = floor(3.14);    // round off down
    double F = fabs(-100);  // absolute value
    double G = log(3);      // logarithm
    double H = sin(45);     // trigonometry
    double I = cos(45);     // ...
    double J = tan(45);     // ...




    // REMEMBER: 
    // %f = float
    // %lf = double
    // %d = integer

    printf("%lf",A);

    return 0;
}
