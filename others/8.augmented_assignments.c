#include <stdio.h>

int main (){
    // augmented assignment operators - used to replace a statement where an operator takes a variable as one of its arguments then assigns the result back to same variables
    //😂😂😂 Dont read the definition.

    // It's just like in python we add "x = x+1"
    // Same stuff

    int x =10;
    x += 10;
    printf("%d",x);

    return 0;
}
